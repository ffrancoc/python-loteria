using Gtk 4.0;
using Adw 1;

template JuegoloteriaWindow : Adw.ApplicationWindow {
  title: "";
  default-width: 800;
  default-height: 650;
  width-request: 800;
  height-request: 650;
  resizable: false;

  Stack stack {
    transition-type: slide_left_right;
  }
}